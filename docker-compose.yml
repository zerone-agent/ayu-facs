services:
  facs:
    build: .
    ports:
      - "8003:8000"
    environment:
      - DEVICE=auto
      - MAX_FPS=10
      - DETECTOR_MAX_EDGE=640
    # GPU support (uncomment to enable)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
